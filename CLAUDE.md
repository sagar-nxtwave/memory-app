@AGENTS.md

# HARD RULES — READ EVERY SESSION

## Git — NEVER push automatically

**NEVER run `git push` unless the user explicitly says "push" or "deploy".**
**NEVER run `git commit` unless the user explicitly says "commit" or "deploy".**

After making code changes: stop. Report what files were changed. Wait for the user to say push or deploy.

This rule has been violated repeatedly and the user is frustrated. No exceptions.
